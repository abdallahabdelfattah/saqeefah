import { PauseDirective } from './pause.directive';

describe('PauseDirective', () => {
  it('should create an instance', () => {
    const directive = new PauseDirective();
    expect(directive).toBeTruthy();
  });
});
